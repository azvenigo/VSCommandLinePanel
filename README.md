# VSCommandLinePanel
Tiny Visual Studio extension to add a command line arguments dockable panel.
Includes quick editing command line parameters and working directory.
Includes a scratchpad that persists with the solution that pops up above the panel when the toggle is set.
